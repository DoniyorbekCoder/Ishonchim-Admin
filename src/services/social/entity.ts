export interface Social {
  id: number,
  url: string,
  priority: number
  image: string,
}
export interface Form {
  id: number | null,
  url: string | null,
  priority: number | null,
  image: string | null
}
